<!-- src: ClickUp "02.3 모두싸인 연동" · doc 2kzn3wbk-318 · 내려받음 2026-07-28 -->
# 02.3 · 모두싸인 연동 설계 (견고성)

## 1\. 연동 방식 선택

모두싸인은 두 가지 서명 요청 방식을 제공한다. **둘 다 쓴다** — 생애주기 커버리지 점수 때문이다.

| 방식 | 용도 | 우리 적용 |
| ---| ---| --- |
| 템플릿 기반 (`POST /documents/request-with-template`) | 고정 서식, 데이터만 동적 채움 | 기부약정서·정기후원 동의·개인정보 동의 — 반복 문서 |
| 임베디드 초안 | 서비스 화면 안에서 업로드·서명자 설정 | 유산기부 약정·Custodian 협조 약정 — 건마다 달라지는 문서 |

인증: API Key를 발급받아 요청 헤더에 Basic 인증으로 전달. Base URL `https://api.modusign.co.kr`.

### 준비 작업 (개발 전 반드시)
1. 모두싸인 웹에서 **템플릿 4종**을 미리 생성하고 참여자 역할·데이터 라벨 설정 → 템플릿 ID 확보
2. 워크스페이스 설정에서 **Webhook 등록** (개발 중엔 cloudflared 터널 URL)
3. `metadata` 필드에 내부 `draft_id`를 넣어 역참조 가능하게 한다 ← 이게 나중에 시간을 크게 아낀다
* * *

## 2\. 어댑터 구조 (ADR-4)

```ts
// lib/modusign/index.ts
export const signer: SignerPort =
  process.env.MODUSIGN_MODE === 'mock' ? mockSigner : realSigner;

interface SignerPort {
  requestWithTemplate(input): Promise<{ documentId, embeddedUrl?, expiresAt? }>
  createEmbeddedDraft(input): Promise<{ documentId, embeddedUrl, expiresAt }>
  getDocument(documentId): Promise<DocumentDetail>
  listDocuments(filter): Promise<DocumentSummary[]>
  resendNotification(documentId): Promise<void>
  cancel(documentId, reason): Promise<void>
}
```

`mockSigner`는 인메모리로 상태를 전이시키고 타이머로 웹훅을 흉내낸다. **API 키가 없어도 전체 흐름이 돈다** — NFR-707(예선 에이전트 3회 독립 실행) 대응이자, 발표 당일 네트워크 사고 보험이다.
* * *

## 3\. 웹훅 처리 — 견고성 8점의 실체

### 알려진 제약과 대응

| 제약 | 대응 |
| ---| --- |
| 웹훅은 문서 ID와 요청자 이메일만 전달 | 수신 즉시 문서 상세 조회 API를 호출해 상태·거절 사유를 보강 |
| 200번대 응답이 아니거나 10초 내 미응답 시 실패 판정, 총 5회 재시도 | 즉시 200 반환 후 비동기 처리. 재시도 대비 멱등성 필수 |
| 이벤트 순서 보장 없음 | 상태 머신 허용 전이 표로 역행 방지 (`SIGNED` → `REQUESTED` 무시) |
| 웹훅 유실 가능 | 폴링 리컨실러로 보정 |
| 임베디드 URL 2시간 만료 | 만료 시각 저장 → UI에서 재발급 버튼 |

### 처리 흐름 (아웃박스 패턴)

```
POST /api/webhooks/modusign
  1. 서명/시크릿 검증
  2. external_event_id로 webhook_events INSERT
     → 중복이면 ON CONFLICT DO NOTHING (멱등성)
  3. 즉시 200 응답            ← 10초 제한 회피
  ────────────────────────────
  비동기 워커 (또는 다음 요청 시 드레인)
  4. 미처리 이벤트 조회
  5. getDocument(documentId) 호출 → 상태·거절사유 보강
  6. 상태 머신 전이 시도 (허용되지 않으면 스킵 + 로그)
  7. 완료 시: PDF 다운로드 → Storage 저장 → sha256 → evidences
  8. Obligation 생성 (정기후원·유언 재검토)
  9. processed_at 기록, audit_logs 기록
```

**부수효과(메일 발송·Obligation 생성)는** **`processed_at IS NULL`** **조건 안에서만 실행한다.** 이것이 FR-503 "정확히 1회" 수락 기준의 구현.
* * *

## 4\. 리컨실러 (FR-504)

```
GET /api/cron/reconcile   — 5분 주기
  1. status = REQUESTED 이고 last_synced_at < now - 5min 인 문서 조회
  2. 각각 getDocument() 호출
  3. 외부 상태 != 내부 상태 → 상태 머신 전이 + 교정 카운터++
  4. last_synced_at 갱신
```

관리자 화면에 **"마지막 동기화 · 교정 건수"** 를 노출한다. 심사위원 앞에서 웹훅을 일부러 끊고 리컨실러가 상태를 맞추는 걸 보여주면 견고성 8점은 사실상 확보된다.
* * *

## 5\. 실패 시나리오 대응표 (NFR-705)

| 상황 | 시스템 동작 | 사용자에게 보이는 것 |
| ---| ---| --- |
| 서명 요청 API 5xx | 로컬 `DRAFT` 유지, 3회 지수 백오프 재시도 | "잠시 문제가 있었어요. 다시 시도할까요?" + 버튼 |
| 임베디드 URL 만료 | 재발급 API 호출 | "서명 링크 유효시간이 지났어요" + \[새 링크 받기\] |
| 참여자 거절 | `REJECTED` + 사유 저장 | 사유 표시 + \[내용 수정해서 다시 보내기\] |
| 웹훅 미도달 | 리컨실러가 5분 내 보정 | 목록에 "확인 중" → 자동 갱신 |
| Upstage API 실패 | 대화는 이어가되 추출 재시도 | "정리하는 데 시간이 걸리네요" + 수기 입력 폼 |
| Document Parse 실패 | 빈 폼으로 전환 | "직접 입력해 주세요" (오류 화면 금지) |

* * *

## 6\. 환경변수

```
UPSTAGE_API_KEY=
MODUSIGN_API_KEY=
MODUSIGN_MODE=real|mock
MODUSIGN_TEMPLATE_DONATION_PLEDGE=
MODUSIGN_TEMPLATE_RECURRING_CONSENT=
MODUSIGN_TEMPLATE_PRIVACY_CONSENT=
MODUSIGN_TEMPLATE_LEGACY_GIFT=
MODUSIGN_WEBHOOK_SECRET=
NEXT_PUBLIC_SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=
```

`.env.example`을 반드시 커밋한다. 예선 에이전트가 실행할 때 막히는 지점 1순위다.
