// 구획 한 겹 — 화면 전체의 결을 여기 한 곳에서 정한다
//
// ⚠ 소유: 화면은 FE 경로다. BE-2가 사람 승인 하에 **새 파일로만** 추가 — 병합 전 FE 리뷰.
//
// 왜 선이 아니라 면인가: 가로줄로 구획을 가르면 서류처럼 보인다. 이 서비스는 서류를
// 다루지만 서류처럼 보일 이유가 없다 — 오히려 무겁고 사무적으로 읽혀서, 마음을
// 정리하러 온 사람에게 어울리지 않는다.
//
// 바탕이 stone-50(따뜻한 미색)이라 구획을 **흰 기 도는 패널**로 살짝 띄우면, 안쪽
// 흰 카드가 한 겹 더 밝게 얹혀 층이 생긴다. 테두리는 있는 줄 알아볼 만큼만 둔다.
//
// ⚠ 값을 화면마다 흩지 않는다. 흩어 놓으면 한 화면만 고쳐지고 나머지는 옛 결로 남아,
//   같은 서비스인데 화면마다 다른 물건처럼 보인다.

/** 기능 하나를 담는 구획. `<section className={SECTION_PANEL}>` */
export const SECTION_PANEL =
  "space-y-3 rounded-2xl border border-stone-200/70 bg-white/60 p-5";

/** 구획들 사이의 간격. 선을 긋지 않고 이 여백이 경계를 만든다 */
export const SECTION_STACK = "space-y-5";
