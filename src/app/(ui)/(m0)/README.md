# M0 화면 — S1~S7

`auth`(S1) · `chat`(S2) · `confirm`(S3) · `rewards`(S4) ·
`doc/[draftId]`(S5·S6) · `vault/[draftId]`(S7)

와이어프레임은 `spec/plan/02.1.1-wireframe.md`. 공통 뼈대는 `app/(ui)/_components/Shell.tsx`
— FR 배지·중단 버튼·터치 44px+·고지 문구를 코드로 고정한다.
