# M2 API — "마음이 축이 된다"

| 폴더 | FR |
|---|---|
| `will/draft/[id]/print/` | FR-302 자필 유언 초안 + 4대 요건 체크리스트 |

⚠ 이 층에는 **서명 경로가 없다.** 유언장은 전자서명으로 효력이 생기지 않으므로
(민법 §1066), 서명 URL을 만들 수 있는 자리를 애초에 두지 않았다.
계약(`HandwritingGuideRes`)에도 서명 필드가 없어 타입이 그것을 강제한다.
