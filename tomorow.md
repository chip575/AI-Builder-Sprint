# 내일 볼 것 (2026-08-03 밤 작업 인계)

> 이 파일은 **밤새 진행한 작업의 인계장**입니다. 아침에 위에서부터 읽으시면 됩니다.
> 🔴 먼저 확인 · 🟡 물어볼 것 · 🟢 다음 구현 순서입니다.

---

## 0. 지금 상태 (한눈에)

```
테스트    534 passed | 1 todo   (57 파일) · **실 DB 계약 스위트 포함**
tsc       0 error
gate      PASS  (신선도 검사 추가됨 — 9항목)
build     성공
템플릿    9/9 콘솔과 일치 (verify-templates.mjs)
DB        recipients 테이블 적용 완료 (db push 했습니다)
브랜치    be2-asset-aware  — 로컬 커밋만, push 안 함
```

**push는 안 했습니다.** 기존 규약대로 요청하실 때 올립니다.

---

## 🔴 0-1. 검토 결과 — **실 DB에서 터질 결함 3개를 찾아 고쳤습니다**

새 StorePort 메서드를 공유 계약 스위트(`store-contract.ts`)에 넣자마자 **실 Supabase가
바로 잡아냈습니다.** 셋 다 인메모리에서는 526개 테스트를 통과했습니다 — FK도 트리거도
없기 때문입니다.

| # | 무엇 | 증상이었을 것 |
|---|---|---|
| 1 | 테이블 이름이 `ledger_nodes`가 아니라 **`intent_ledger_nodes`** | 배포 후 철회 누르면 500 |
| 2 | `subject_id`는 draftId가 아니라 **intentId** (FK → `intents.id`) | FK 위반 500 |
| 3 | `intent_ledger_nodes`는 **append-only** — 트리거가 UPDATE를 막는다 (NFR-704) | 철회가 아예 불가 |

**3번이 설계 문제였습니다.** 철회를 "지난 노드의 status를 바꾸는 일"로 만들었는데,
원장은 고칠 수 없는 장부입니다. **철회 노드를 쌓는 일**로 바꿨습니다 — 마지막 노드가
`REVOKED`면 체인 전체가 닫힙니다. 지난 노드의 해시는 손대지 않습니다.

> 오히려 이쪽이 맞습니다. "언제 왜 철회했는가"가 노드로 남고, append-only라는
> 원장의 존재 이유도 지켜집니다.

### 여기서 배운 것 (규칙으로 남기시면 좋겠습니다)

> **StorePort에 메서드를 더하면 `store-contract.ts`에도 넣는다.**
> 안 넣으면 인메모리만 검사되고 실 DB 경로는 아무도 안 본다.
> 증상은 에러가 아니라 **배포 후 500**이다.

`AGENTS.md`나 `docs/decisions.md`에 한 줄 넣어두시길 권합니다.

### 곁가지로 고친 것

- `revocationRule`이 모르는 `DocType`을 받으면 화면이 죽었습니다 (`rule.kind` of undefined). 화면이 `as DocType`으로 캐스팅해 넘겨서 타입이 안 지켜줍니다 → "모른다"로 답하게 했습니다
- 지킴이 초대가 **매번 빈 세션**을 만들고 있었습니다 (발화 0건 고아 intent)
- CLM 응답에 `intentId`를 실었습니다 — 화면이 철회·이력을 올바른 id로 부릅니다

### 🔴 아직 남은 같은 계열 — `/estate`

`/estate`가 `obligations.subjectId`(= **draftId**)를 `/api/ledger/${id}`(= **intentId** 기대)에
넘기고 있습니다. `.catch(() => null)`로 삼켜서 **조용히 아무것도 안 뜹니다.**
"뜻이 바뀐 기록"이 빈 채로 보이는 원인입니다. 지금 고치지 않은 이유는 FE 소유 경로의
데이터 흐름을 바꿔야 해서입니다 — 아침에 함께 보시죠.

---

## 🔴 1. 아침에 먼저 확인할 것

### 1-1. 실 DB에 `recipients`가 들어갔습니다

`npx supabase db push`를 돌렸습니다. **되돌리려면** `drop table public.recipients;` 입니다.
추가만 하는 마이그레이션이라 기존 데이터에 영향은 없습니다. RLS 켜고 정책은 두지 않았습니다(D-18 — 클라이언트 전면 차단).

> 확인: Supabase 대시보드 → Table Editor에 `recipients`가 보이는지, RLS 배지가 켜져 있는지.

### 1-2. 철회 통지 **실제 왕복을 안 해봤습니다**

코드는 다 됐고 테스트도 통과하지만, **모두싸인으로 실제 발송을 한 번도 안 했습니다.**
조회 중에 429(호출 한도)를 한 번 맞아서 그 뒤로 자제했습니다.

확인 순서:
1. 마이페이지 → "알릴 분"에 **기관**을 하나 등록 (본인이 받을 수 있는 이메일로)
2. CLM → 유산기부 약정 줄 → `철회하기` → 사유 적고 → 받으실 곳 고르고 → 실행
3. 메일함에 **두 통**이 오는지 (본인 1순위 → 기관 2순위 순서라 본인 것 먼저)
4. 서명 후 CLM 상태가 도는지

**예상되는 실패 지점**: `original_agreement` 칸에 `LEGACY_GIFT_AGREEMENT · 2026-08-03 체결`처럼 **코드값이 그대로 인쇄**됩니다. 사람이 읽을 이름(`유산 기부 약정서`)으로 바꾸는 게 맞는데, 라벨 표가 화면(`DOC_LABEL`)에만 있어서 서버가 못 씁니다. → 아래 🟢 4-3

### 1-3. `AssetPeek`이 세 화면에 붙었습니다

`/chat` · `/confirm` · `/write`. 전부 **접혀 있고** 누르면 열립니다.
`/estate`의 "자산 상태 점검"과 같은 컴포넌트를 씁니다.

> 확인: 세 화면에서 눌러 보고, 자산이 0건일 때 **"저희가 확인한 자산이 없습니다"** 가 나오는지. ("등록된 자산이 없습니다"가 나오면 잘못된 겁니다 — 그건 "당신은 재산이 없다"로 읽힙니다.)

### 1-4. 마음 유언 서류화

`/heartwill` 아래쪽에 **"서류 이력에 넣기"** 가 생겼습니다. 누르면 CLM에 `마음 편지`로 뜹니다.

> 확인: 문단을 승인하지 않고 누르면 버튼이 회색이고 이유가 뜨는지. 두 번 눌러도 CLM에 하나만 뜨는지.

---

## 🟡 2. 물어볼 것 (제가 임의로 정한 것들)

권장으로 처리했습니다. 아니면 말씀해 주세요.

| # | 정한 것 | 왜 | 되돌리기 |
|---|---|---|---|
| 2-1 | **유가족 이메일을 그냥 받습니다** | 해커톤이라 하셔서. 다만 제3자 정보라 화면에 "알려 드릴 일이 있을 때만 씁니다" 고지를 넣었습니다 | 고지 문구만 빼면 됨 |
| 2-2 | **C안(링크+확인코드)은 버튼만** | 확인 코드를 보내려면 메일 인프라가 필요한데 없습니다. Supabase Auth OTP를 쓰면 유가족 **계정이 생겨** 사실상 B안이 됩니다 | 열람 매트릭스와 함께 다시 |
| 2-3 | **철회 통지서 게이트 = `ESIGN_OK`** | 게이트는 "이 서명이 유효한가"를 판정하지 "필수인가"를 판정하지 않습니다. 서식 제2조가 본문에서 "서명 여부와 무관하게 성립"이라 말합니다 | `validity-gate.ts` 한 줄 |
| 2-4 | **철회 통지서를 2인 서식 예외로** | 기존 검사가 "참여자 2인이면 문서가 완료되지 않는다"고 막고 있었습니다. 여기서는 미완료가 **결함이 아니라 사실**이라 예외로 뒀습니다 | `template-labels.test.ts` |
| 2-5 | **신선도 경고는 FAIL이 아니라 WARN** | 확인이 늦었다고 배포를 막을 일은 아니고, 보이는 것이 목적입니다. 지금 180일 기준 | `gate-check.sh` `STALE_DAYS` |
| 2-6 | **`recipients`는 개인 주소록** | `user_id`로 묶습니다. 나중에 "부산 지역 기관 디렉터리"를 만들면 **별개 테이블**이어야 합니다 | 지금 정해두면 충돌 없음 |

### 🟡 법률 검토가 필요한 것 (제가 판단할 수 없음)

- **유가족 이메일 = 제3자 개인정보** (개인정보보호법 §15). 본인 동의를 받을 수 없는 자리입니다. `legal-basis.md` §6.4에도 §10 미검증 목록에도 이 경우가 없습니다.
- **철회 통지서 제7조**가 「개인정보 수집·이용 동의서」를 가리키는데, **기관은 그 문서에 서명한 적이 없습니다.** 존재하지 않는 문서를 참조하는 조항입니다. 실서비스라면 "각자의 개인정보 처리방침에 따른다"로 바꾸는 게 정확합니다.
- **§1112 제1~3호 개정 경과** — 2026. 2. 12. 통과분의 공포일·시행일·소급범위가 자료마다 갈립니다 (`legal-basis.md` §10). 그래서 유류분은 **"있다"까지만** 말하고 계산하지 않습니다.

---

## 🟢 3. 밤새 만든 것 (커밋 순서대로)

| 커밋 | 내용 |
|---|---|
| `f925c90` | 시스템 프롬프트를 `lib/ai/prompts`로 분리 — 스냅샷 확보 (동작 불변) |
| `b5c4da8` | 프롬프트에 **보안 금지 조항** + 서류별 주의 |
| `530b5ad` | 대화가 **재산을 읽는다** — 매 턴 주입, 기부/유산 반대 지시 |
| `e68f8f1` | `/estate` **자산 상태 점검** 버튼 + 합계 규칙 공용화 |
| `9ce8270` | **법률·세무 안내 경계** + 문의처 (132 · 126 · 고향사랑e음) |
| `95d3fd2` | **추천 질문 칩** + 안내 커버리지 계측 |
| `aa43138` 🔒 | `Recipient` 계약 |
| `28cfd89` | 알릴 분 관리 (마이페이지) |
| `a3b3f58` 🔒 | 철회 계약 · **§1112 정정** · **§1108 추가** · 철회 가능 표 |
| `1356294` | 철회 API — 원장 `REVOKED` |
| `5e6ff46` | 철회 화면 · 통지 발송 · `/ledger` 진입점 |
| `?` | 마음 유언 서류화 · `AssetPeek` · 법령 신선도 검사 |
| `1f1f019` | **지킴이 초대·회수** — 서명해야 열람이 열린다 |
| `c0ee846` | 지킴이 화면 |

🔒 = **보호 경로**(`lib/contracts`·`lib/rules`). 훅을 우회해 적용했습니다 — 승인해 주신 대로입니다. **커밋을 따로 뗐으니 `git show <해시>`로 diff만 보실 수 있습니다.**

### 밤새 고친 실제 결함

1. **§1112가 낡아 있었습니다** — 형제자매 유류분은 헌재 2024. 4. 25. 2020헌가4로 **효력을 잃었는데** 우리는 여전히 "상속인에게는"이라고 안내했습니다. 방향이 반대였습니다: 미혼·무자녀인 분에게 **없어진 위험을 경고**하고 있었습니다.
2. **사인증여 철회권을 한 마디도 안 했습니다** — `legal-basis.md` §1 표가 "단서 필수"라고 못박아 뒀는데 빠져 있었습니다.
3. **`SignerPort`가 참여자를 1명만 보냈습니다** — `FAMILY_ACK`이 2인 역할인데도요.
4. **`debtNotice`가 어디에도 안 보였습니다** — 서버가 계산해 내려보내는데 화면이 안 그렸습니다.
5. **안내층에 세법 주제가 없었습니다** — 세금 질문이 LLM으로 그대로 샜습니다.
6. **`/ledger` 진입점이 없었습니다** — 화면은 있는데 갈 길이 없었습니다.

---

## 🟢 4. 다음에 구현할 것 (우선순위 순)

### 4-1. ~~지킴이 초대~~ — **밤새 만들었습니다**

`/estate`에 "지킴이" 섹션이 생겼습니다. 확인해 주십시오:

1. 마이페이지 → 알릴 분 → **지킴이**로 한 분 등록
2. `/estate` → 지킴이 → 고르고 · 성함 적고 · 범위 체크 → `협조를 부탁드리기`
3. 그분 메일로 **협조 약정서**가 가는지
4. 서명 전에는 `수락 기다리는 중`, 서명 뒤에 `열람 가능`으로 바뀌는지

**설계상 중요한 것**: 초대만으로 열람이 열리지 않습니다. 웹훅 `COMPLETED`에서만
`grantedAt`이 찹니다 — 상대가 수락 안 했는데 권한이 생기면 **사용자는 맡긴 줄 알고
상대는 맡은 줄 모르는** 상태가 됩니다.

거둔 권한은 **뒤늦게 온 서명 웹훅으로도 되살아나지 않습니다.** `grantedAt`은 지우지
않습니다 — "언제 열렸다가 언제 닫혔나"가 남아야 합니다.

> ⚠ 아직 안 만든 것: **열람 범위 변경**(`CustodianScopePatch`). 범위를 넓히면
> 약정서 본문이 바뀌므로 MATERIAL → 재서명이어야 합니다 (NFR-713). 지금은
> 재초대로 덮어쓰면 PENDING으로 돌아가는 것까지만 됩니다.
>
> ⚠ 그리고 **열람 자체가 아직 없습니다.** 권한은 열리는데 지킴이가 볼 화면이
> 없습니다 — 그건 열람권 매트릭스와 함께 만들 일입니다.

### 4-2. `/estate`의 사용자 필터 없음 — **보안**

화면 머리 주석에 이미 적혀 있습니다:

> `GET /api/obligations/fire`는 관리 화면용이라 **사용자 필터가 없다**

지금은 단일 사용자라 안 터지는 것뿐이고, **다계정에서 남의 약속이 보이는 모양**입니다. 사용자 범위 목록 API가 필요합니다.

`GET /api/ledger/[subjectId]`에도 소유 확인이 없습니다(철회 라우트에는 넣었습니다). 유족 열람 경로가 아직 설계 안 돼서 열어 둔 상태인데, **열람권 매트릭스와 함께 닫아야** 합니다.

### 4-3. 문서 표시명을 서버가 쓸 수 있게

`DOC_LABEL`(기부 약정서·유산 기부 약정서…)이 **화면에만** 있습니다. 그래서 철회 통지서의 `original_agreement` 칸에 코드값이 그대로 인쇄됩니다.

`lib/rules/branch-doc.ts` 옆이나 `lib/contracts/common.ts` 근처로 옮기고 양쪽이 같은 표를 쓰게 하면 됩니다.

### 4-4. 안내 카드 (계측 결과 보고)

`[guide] MISS — 질문형인데 주제 없음` 로그가 쌓이고 있습니다. **며칠 쓰신 뒤** 상위 항목을 카드로 만드시면 됩니다. 짐작으로 40개 쓰면 절반은 아무도 안 묻는 걸 씁니다.

> RAG는 하지 않기로 했습니다. `legal-basis.md` §6.3의 변호사법 §109 방어선이 *"법률상담이 아니라 ① 법령 정보 제공 ② 서식 초안 ③ 자필 요건 체크리스트"* 인데, **질문에 맞춰 문서를 찾아 답을 구성하는 것은 그 경계를 넘습니다.** 기준선: 카드가 200개를 넘고 주제 규칙이 서로 겹쳐 유지가 안 될 때 다시 봅니다.

### 4-5. 자잘한 것

- `legal-basis.md` §4.3 **철회 이력 보존** 체크박스 → 이제 됩니다 (원장에 남습니다). 체크하시면 됩니다.
- `legal-basis.md` §10 미검증 8건 — 특히 **Upstage 데이터 처리 위치**(국외면 §28의8 고지·동의 필요, 지금 동의 화면에 그 항목 없음)
- 지자체별 기부 문의처 — `lib/referral/registry.ts`에 항목만 추가하면 됩니다. 실제 주소를 주시면 확인해서 넣겠습니다

---

## 5. 새로 생긴 파일 지도

```
src/lib/ai/prompts/
  system-prompt.ts          시스템 프롬프트 유일 출처 (4층 조립)
  asset-readback.ts         재산 문장 — 코드가 만들고 모델은 그대로 싣는다
  __snapshots__/            조립 결과 전문 ← PR diff에서 이걸 보세요
src/lib/ai/session/
  suggested.ts              추천 질문 칩 (guide에서 파생)
src/lib/referral/registry.ts  문의처 (source.url + verifiedAt 강제)
src/lib/estate/rollup.ts      채무 분리·합계 규칙 (대화와 화면 공용)
src/lib/rules/revocation.ts   취소·철회·해지·회수 판정
src/lib/contracts/recipient.ts
src/app/api/(m0)/recipients/route.ts
src/app/api/(m2)/heartwill/document/route.ts
src/app/api/(m3)/ledger/[subjectId]/revoke/route.ts
src/app/api/(m3)/ledger/[subjectId]/notify/route.ts
src/app/(ui)/_components/AssetPeek.tsx
src/app/(ui)/(m4)/estate/AssetStatus.tsx
src/app/(ui)/(m4)/clm/RevokeCell.tsx
src/app/(ui)/(m0)/mypage/RecipientBook.tsx
src/app/(ui)/(m2)/heartwill/SaveAsDocument.tsx
src/app/(ui)/(m4)/estate/CustodianBook.tsx
src/app/api/(m4)/estate/custodians/route.ts
```

---

## 6. 잊지 마실 것

- **`.env`에 `MODUSIGN_TEMPLATE_REVOCATION_NOTICE`를 넣었습니다.** 배포 환경변수에도 넣으셔야 실 서비스에서 통지가 나갑니다.
- **`legal-basis.md`를 git에 넣었습니다.** 확인일이 적힌 문서가 버전 관리 밖에 있었습니다.
- 프롬프트를 고치시면 **`__snapshots__` diff를 함께 보십시오.** 조각이 아니라 조립 결과가 리뷰 대상입니다. 의도한 변경이면 `npx vitest -u`.
