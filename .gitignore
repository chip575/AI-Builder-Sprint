# deps / build
node_modules/
.next/
dist/
*.tsbuildinfo

# secrets — .env.example만 커밋한다 (02.3 §6)
.env
.env.*
!.env.example

# misc
.DS_Store
*.log

# 사용자 작업 폴더 (레포 외 문서)
현재 click up파일 수정 예정/

# 작업용 아카이브·산출물 — 레포에 올리지 않는다 (심사자에게 노출되는 위치다)
*.zip
*.7z
*.rar
*.tar
*.tar.gz
batch.log
.vercel
.env*
batch/
