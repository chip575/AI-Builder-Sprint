# AI 활용 로그

매일 각자 한 줄. 형식: `날짜 | 담당 | 무엇을 AI에 맡겼고, 무엇을 사람이 고쳤는지`
대회 끝나고 몰아 쓰면 기억나지 않는다 (사전교육 2-11 ⑥).

2026-07-29 | PM | 계약 24모듈 Zod 스키마·레포 초기화를 AI가 수행. 사람이 고친 것: update-manifest 파이썬 스텁 버그, 아카이브 검사 오탐. 계약 필드 명명은 팀 리뷰 필요.
2026-07-29 | PM | M-SIGN~M-REWARDS·StorePort·스키마를 AI가 구현, 사람이 검토 지적(RPC 옆문·UPSERT WHERE·테스트 비현실성 등)을 반영. 레포 평탄화.
2026-07-30 | agent | ? | (무엇을 AI에 맡겼고 무엇을 사람이 고쳤는지 채우세요)
2026-07-30 | PM | M1(카운터·지표·리컨실러·종이스캔)·M2(필사 가이드)를 AI가 구현. 사람이 잡은 것: 판례 근거 표기(P3), 빈칸 유도 위치, 배포 여부 판단. AI가 잡은 것: 증빙 경합·웹훅 이벤트명·cron GET.
2026-07-30 | AI(Claude) | mock signer 외부 상태를 store로 분리(서버리스 인스턴스 교체 대비) — 첫 시도가 draft를 갱신해 웹훅 유실 재현을 깨뜨린 것을 테스트가 잡아내 되돌림. 사람: 배포 env 입력·재배포
2026-07-30 | AI(Claude) | 배포 웹훅 실측 검증(200·401·멱등) 중 fire-and-forget 드레인이 서버리스에서 실행되지 않는 것을 발견 — after()로 교체. 사람: 모두싸인 콘솔 웹훅 등록
2026-07-31 | AI(Claude) | M2 질문은행(5축 20문항)·커버리지 엔진·회상 화면 구현. 사람: lib/rules 경로 승인(소유권 훅이 차단 → PM 승인 후 진행), 승인 근거를 파일 헤더에 기록
2026-07-31 | AI(Claude) | M2 마음 유언(FR-111) — StorePort 문단/버전 3메서드·POST /api/heartwill/apply·문단 승인 화면 구현. AI가 잡은 것: 마음 유언 테이블 미적용 프로젝트에서 공유 계약 스위트가 통째로 깨지는 것(테이블 존재를 실제로 물어보고 그 블록만 skip), 서버 컴포넌트의 저장소 실패 시 빈 화면이 "이야기 없음"으로 읽히는 문제. 사람: FR-112 전달 설정(PATCH /api/heartwill/delivery) 판단 보류 — 지시서 구현 목록 밖이고 수신자 스키마가 없다.
2026-07-31 | AI(Claude) | M-LEDGER 최소 구현(해시 체인·실질성 판정·최신성 유도·StorePort 2구현·POST /api/ledger). 순수 로직 3케이스만 테스트. 사람 확인 필요: 마이그레이션 미적용, 라우트 경로가 manifest(POST /api/ledger/node)와 다름, MATERIAL 재서명·가족 인지는 미구현.
2026-07-31 | AI(Claude) | 유족 타임라인(GET /api/ledger/[subjectId] + 화면) 구현 — 밤 배치가 브랜치 전환으로 건너뛴 03. StorePort.recordAudit 추가(열람 기록)
2026-07-31 | AI(Claude) | M-EVAL 픽스처 12케이스·실행기 구현, 실측 12/12. 첫 측정 41.7%는 모델이 아니라 하네스 결함(필수 슬롯 미전달·타임아웃 없음)이었다 — 그 사실 자체를 결과에 남김
2026-07-31 | AI(Claude) | M-FAMILY-ACK 구현 — 가족 응답을 별도 API가 아닌 서명 웹훅으로 받게 설계(인지 주장에 서명 근거가 붙도록). solar-responder에 연결 타임아웃 부재를 발견해 추가
2026-07-31 | AI(Claude) | M-EMBEDDINGS 구현 — 임베딩 차원을 문서 대신 실제 호출로 확인(4096), 그 결과 pgvector ANN 인덱스 상한(2000) 밖임을 발견해 순차 스캔으로 설계
2026-07-31 | AI(Claude) | M1 잔여 3종(OBLIGATIONS·TIMETRAVEL·ADMIN-OPS) 구현. SIGN-LIFECYCLE은 계약(RemindReq) 부재로 제외 — 서식 잔여 건수(10)를 감안해 계약 신설도 보류
2026-07-31 | AI(Claude) | WORKER_ROLE 훅 실검증(11케이스) — 라우트 그룹 도입 후 역할 패턴이 안 맞아 be1·be2가 자기 경로에서 차단되던 것을 발견해 수정. 정상 통과 케이스를 함께 재지 않았으면 못 잡았다
2026-07-31 | AI(Claude) | 계약 4건 적용(estate 신설·remind 추가·DocType/판정 1건) + branch_proposals.status 영속화. B트랙 워커가 금지 경로에 막혀 미완으로 보고한 지점을 사람(=PM 경로)이 이어받음
2026-07-31 | AI(Claude) | M-SIGN-LIFECYCLE 리마인드 구현 — 임계 기준점을 draft 생성이 아닌 외부 문서의 발신 시각(startedAt)으로 잡고, 완료·거절 판정도 로컬이 아닌 외부 상태로 함(웹훅 유실 시 오발송 방지). remindCount는 StorePort에 감사로그 되읽기가 없어 프로세스 사본으로 셈 — 사람 판단 필요
2026-07-31 | AI(Claude) | C트랙 병합 후 보완 — 리마인드 간격을 '직전 발송 기준'으로 바로잡음(요청 시각만 보면 임계 통과 후 연속 발송이 열려 독촉이 된다, FR-113). 발송 횟수를 프로세스 사본에서 audit_logs 조회로 이전
2026-07-31 | AI(Claude) | 민법 §1019 상속 승인·포기 기간을 lib/rules에 추가(PM 승인). D-day를 계산하지 않는 것과 전문가 상담 권고를 테스트로 고정 — 기산점이 사망일이 아니라 '안 날'이라 우리가 알 수 없다
2026-07-31 | AI(Claude) | M4 ESTATE 4a 인벤토리 구현(스키마 3표·StorePort 2구현·POST/GET /api/estate/assets·beneficiaries·assets/scan). 파이프라인은 한 줄도 고치지 않았다 — 판독은 M-PAPER-SCAN의 DP+IE와 업로드 보관소를 그대로 재사용. 마스킹을 라우트가 아니라 저장 계층에 둠(호출부를 하나라도 빠뜨리면 원문이 앉으므로). 사람 확인 필요: 마이그레이션 미적용(db push 금지), mask.ts는 보안 5조 리뷰 대상, 채무 안내(민법 §1019)는 lib/rules 부재로 debtNotice=null
2026-07-31 | AI(Claude) | A트랙(M4 인벤토리) 병합 + §1019 연결. 워커가 남긴 debtNotice=null 기대값을 룰 생성 후 갱신 — 지시가 임시 상태였던 것을 테스트가 붙잡고 있었다
2026-07-31 | AI(Claude) | 서식 8종(v3) 필드 검증층 template-fields.ts 신설 — 칸 넘침·서식별 금칙·서면 정합성. 주민번호 검사는 gate:check(소스 grep)·maskIdentifier(DB 저장)와 다른 관문이라 중복이 아님. 통과 케이스가 연락처를 계좌로 오판하던 것을 잡아냄
2026-07-31 | AI(Claude) | 대화 응답기에 세션 지식 주입 — 이미 말한 슬롯을 되묻던 문제 수정(eval 41.7%와 같은 계열: 코드가 아는 것을 프롬프트에 안 넘김). 축 세션이 질문은행을 쓰도록 연결, 옛 테스트가 버그를 고정하고 있던 것을 정정
2026-07-31 | AI(Claude) | 대화 화면 이어쓰기 — sessionId가 화면 상태로만 있어 새로 들어오면 지난 이야기로 갈 길이 없던 문제 수정. 저장 표시는 서버 커버리지를 그대로 쓴다(화면이 세지 않는다)
2026-07-31 | AI(Claude) | 막다른 화면 제거 — 확인 화면에 돌아가는 길이 없고 필수 항목이 비면 버튼까지 죽어 회색 버튼만 남던 문제. Shell에 back을 넣어 다음 화면도 자동으로 갖게 하고, 화면 전수 검사를 테스트로 고정
2026-07-31 | AI(Claude) | 가지 결정 라우트 완성 + 제안 상태를 영속 경로로 일원화(메모리 저장소 제거). ai-usage.md에 병렬 에이전트 운영 절 추가 — 성공뿐 아니라 역할 배정 오류·빈 마이그레이션·틀린 지시서까지 기록
2026-08-01 | AI(Claude) | 서식 dataLabel 번역층 + 대조 스크립트. 조회(무료)로 실측해 셋을 잡음 — ① 모두싸인 Basic 인증의 콜론 위치가 반대여서 실서명이 전부 400이었을 것 ② DocType과 서식 코드가 5종에서 달라 8종 중 5종이 요청 불가였음 ③ .env 파서가 빈 값에서 다음 줄을 집던 버그. 서명 1건도 쓰지 않았다
2026-08-01 | AI(Claude) | 템플릿 8종 조회 대조 완료(서명 0건 사용). 죽은 라벨 0, 초과 5종은 같은 역할 안의 칸이라 안전. ⑦ 가족인지확인서에서 가족 서명란이 본인 참여자에 배정돼 가족 필드가 0개인 것을 발견 — 본인이 가족 서명까지 그리게 되는 상태(FR-554 방어선 붕괴). 콘솔 수정 필요
2026-08-01 | AI(Claude) | 프론트 연결 점검을 에이전트에 위임(링크 그래프 + dev 서버 실HTTP, 브라우저 없음은 미확인 항목으로 분리) → docs/fe-gaps.md. 화면 15개 중 8개가 도달 불가였다. 사람이 정한 것: 게이트 순서 교체·인쇄 종착·집계 규칙 변경. 셋 다 "각 부분은 정상인데 조합이 안 되던" 종류라 유닛 361개가 전부 통과한 채로 숨어 있었고, e2e 한 단계를 추가해서야 고정됐다(차단 카운터 0→13). lib/contracts는 훅이 막아 PM 수동 적용 대기
2026-08-01 | AI(Claude) | FR-509 집계 규칙을 코드·계약 주석·decisions(D-20)로 일치시킴. 옛 규칙이 적힌 곳이 세 곳이었고 그중 D-18 원문은 지우지 않고 포인터로 이었다. 부수 발견: 소유권 훅이 Edit/Write/MultiEdit만 검사해 Bash 경유 수정은 통과한다 — 사람이 "위협 모델은 실수 방지이므로 오탐 위험이 큰 Bash matcher는 넣지 않는다"고 판단, 알려진 한계로 AGENTS.md에 문서화하고 최종 방어선을 git diff·병합 전 리뷰로 명시
2026-08-01 | AI(Claude) | 대화가 "무슨 말을 해도 같은 질문"이던 원인 셋을 분리: ① 질문은행에 askedIds를 안 넘겨 같은 질문이 영구 반복 ② 응답기 규칙이 그 질문을 그대로 쓰도록 강제 ③ 가지 제안 SSE를 화면이 버림. 구조 원인은 detectExpress가 3분기인데 소비하는 쪽이 2분기만 알아 UNCERTAIN이 바닥으로 떨어진 것 — 사람이 "규칙은 확신만 잡고 나머지를 넘기라고 있다"로 판단해 UNCERTAIN을 확인형 제안으로 승격. lib/rules 3줄(UNCERTAIN에 branchType 동반)은 PM 승인 후 적용, 판정 결과는 불변. 별도 발견: 배포본 UPSTAGE_MODE=mock이라 LLM이 호출조차 안 되고 있었다
2026-08-01 | AI(Claude) | 실 모드(UPSTAGE=real·DATA=supabase) 첫 관통에서 추출 신뢰도가 전부 LOW로 떨어지는 것을 발견 — 실 Solar가 "원문 조각" 지시와 달리 발화를 통째로 주거나 정규화해 돌려주는데 findSpan이 정확 일치만 해서 근거를 잃고 있었다. 값으로 다시 찾는 폴백을 넣어 어림 표현이 설계대로 MEDIUM에 남게 함(0.3→0.6), 지역은 조각으로 좁혀 잡힘(0.3→0.95). 폴백이 없는 근거를 만들지 않는 것도 테스트로 고정(쌍 검사 3건 추가). mock에서는 드러나지 않던 결함 — ai-usage.md §3의 신뢰도 표는 실 모드에서 성립하지 않고 있었다
2026-08-01 | AI(Claude) | 유산 안내 층 신설(FR-101 확장) — 질문형 발화에 LLM 없이 코드가 답한다. 문구·수치는 전부 lib/rules 읽기(P3), 재촉 표현 금지를 테스트로 고정(P4), 의사 표현은 express에 넘기는 쌍 검사 포함. 실서버 확인: 안내 3종에 조문·확인일자 병기, Express 경로 무영향. 발견 2건은 사람 판단 대기 — ① "유언장은 어떻게 남기나요?"(질문형)가 EXPRESS로 직행(express-detect는 보호 경로) ② FE 사이드바 개편(a436c4e)이 chat-resume 테스트 2개를 깨뜨림(회귀 아님, 문자열 검사가 낡음 — FE 소유라 미수정)
2026-08-01 | AI(Claude) | 브라우저 실연동 시연(질문→안내→가지 수락→슬롯→확인→답례품→게이트→서명→증빙 해시) 관통. 실 Solar가 "오백만원"을 500,000으로 오추출·후원 대상에 질문 문장을 채웠으나 신뢰도 층이 배지로 잡아 사용자가 인라인 수정(설계 작동, FR-102/103). 발견: 폴링이 웹훅보다 먼저 COMPLETED로 동기화하면 증빙이 영원히 안 생긴다 — ensureEvidence는 웹훅 도착이 전제고 리컨실러는 REQUESTED만 스캔(FR-504·505 갭, mock 흐름은 데모 버튼 숨김(c8a74d3) 이후 항상 걸림). 수정은 BE-2 소유 경로라 보고만: 리컨실러에 "COMPLETED인데 증빙 없음" 스캔 추가 또는 상태 폴링의 COMPLETED 전이 시 ensureEvidence 호출 제안
2026-08-02 | AI(Claude) | 서류 작성실(/write) 신설 — 문서 카드 선택(=Express 직접 진입)→Solar 슬롯 대화→약정서 미리보기 빈칸 채움→기존 confirm/doc으로 합류. 유언장 카드는 서명 버튼 없이 조문+필사 가이드만(절대 규칙 4). FE의 shell.test(모든 화면 Shell 강제)가 첫 판을 잡아 Shell 적층 구조로 정정 — 쌍 검사가 남의 실수도 잡았다. 라우트 2건: 질문형 발화는 Express 직행 대신 안내가 먼저(FR-115B 취지), 열린 가지는 턴을 넘어 유지(둘째 턴부터 회상으로 새던 것). 실측 발견: "유산을 기부"는 조사 때문에 express LEGACY target(유산\s*기부)을 놓쳐 DONATION으로 떨어진다 — 규칙은 보호 경로라 발화를 패턴에 맞추고 주석으로 남김. API 검증: LEGACY 직행→ACCEPT→PENDING_RECONFIRM(숙려)→PROCEED_TODAY→OPENED
2026-08-02 | AI(Claude) | 사람(BE-1) 허락 하에 FE 경로 3건 — ① 랜딩 CTA를 작성실(/write)로, 회상(/chat)은 보조 문으로(피벗 반영, FR-110 긴장은 PM 검토로 주석 명시) ② /write·/guide를 미들웨어 보호에 추가(발화 저장 화면은 /chat과 같은 급) ③ 사이드바 개편(a436c4e)이 깨뜨린 chat-resume 테스트 2건을 리팩터된 실체에 맞게 갱신 — 의도(서버 기준 잠금·문구 보존)는 유지, 위치만 이동. 42/42 파일 초록 복구
2026-08-02 | AI(Claude) | 작성실 피드백 반영 — ① 제4조를 법률어("상속인·유류분")가 아니라 쉬운 말이 본문이 되게 고침(조문 원문은 근거 줄 유지, P3) ② 미리보기를 채팅에서 분리: 대화 탭에는 채움 현황 칩만, 문서는 "약정서 보기" 탭으로. 말을 보내면 대화 탭으로 자동 복귀. 42/42 유지
2026-08-02 | AI(Claude) | "내 유산"(/estate) 대시보드 신설 — 사용 피드백("유산 관리가 아니라 유산기부 시스템 같다")의 원인이 관리 기관(자산·약속·이력)이 백엔드에만 있고 화면이 없던 것. 다가오는 약속(체결 시 자동 생성된 6개월 재검토가 실데이터로 표시)·남긴 약정(서명 상태+증빙 링크)·자산 손입력/종이 등록·뜻이 바뀐 기록 4구획 + 시간 압축 데모 버튼. 한계를 화면 주석에 명시: 약정 목록 API 부재(약속 걸린 문서만 역추적 표시), obligations GET에 사용자 필터 없음(다계정 전 BE-2 필요, NFR-714). 랜딩에 "내 유산" 문 추가, /estate 미들웨어 보호
2026-08-02 | AI(Claude) | 작성실 문서 진열 확장 — "유산기부 시스템 같다" 후속. 이미 배선된 두 가지(문화유산 후원 서식⑤·자산 지킴이 서식⑧)를 API로 관통 확인 후 카드 추가, 지금/사후/정리·맡김 3묶음으로 재편. 지킴이 미리보기에 "유언집행자 아님" 고지를 본문 필수로(00.2 §7.1, 게이트 주석 준수). 상속은 안내 카드로 정직하게 — 분할협의서는 인감 실무 때문에 안 만든다고 문 앞에서 말하고 /guide로 연결. 상속 문서 신설은 PM 계약 제안(PR #5 코멘트) 대기 유지
2026-08-02 | AI(Claude) | 자산 지킴이 약정서를 작성실 진열에서 제거(사람 결정) — 카드·미리보기·현황 칩만 걷어냄. ESTATE 가지·게이트 판정·서식 ⑧ 배선은 그대로 두어 자산 정리 흐름(M4)이 자라면 그쪽에서 다시 건다
2026-08-02 | AI(Claude) | 정체성 교정(사람 결정 A·B) — ① 홈 반전: 랜딩 메인이 "내 유산 정리 시작하기"(/estate), 서류 준비는 안쪽 문으로. 관리 서비스인데 작성 퍼널이 대문이던 것을 뒤집음 ② 자산→약정 연결: 자산 카드 "이 자산 남기기" → 작성실이 그 자산을 첫 문장에 싣고 유산 기부 흐름 자동 진입(배너 표시, StrictMode 이중 세션 방지). 실확인: 예금 카드 클릭→LEGACY 직행→숙려 카드까지 관통. 자산 변화 이력·약정 연쇄(C)는 스키마 필요 — PM·BE-2 제안으로 남김
2026-08-02 | AI(Claude) | 숙려 도돌이표 수술 — 원인은 solar-responder의 LEGACY 목표 문구("오늘 진행할지 고르시게 한다"): 결정은 decide 버튼만 받는데 대화가 물어서, 채팅 "네"가 상태를 못 바꾸고 무한 반복. 두 층 수리: ① handoff.ts 신설 — 다음 행동이 화면(숙려 버튼·확인 화면)에 있으면 LLM을 건너뛰고 코드가 그 문을 가리킴 ② Solar 목표에서 해당 지시 제거+경위 주석. 쌍 검사 7건(잡는 4·넘기는 3). 실서버 재현: 숙려 대기 중 "네"→버튼 안내 고정, 열림 후→확인 화면 인계
2026-08-02 | AI(Claude) | "말해도 약정서에 없다" 수리 — 응답기는 매 턴 값을 되읽는데 그 값이 추출 전까지 어디에도 저장되지 않아 미리보기가 비어 있었다. 라우트가 턴마다 훑은 값을 기존 facts 저장소에 바로 저장(계약 밖 채널 아님, 확정값 보호는 StorePort), 작성실은 meta마다 자동 갱신 — "부산에 삼십만원" 발화 직후 칩·약정서 채움 실확인. 세션 관리는 절반만: 이어쓰기 안내 문구+값 복원까지. 대화 원문 복원·세션 목록은 조회 계약이 없어 불가(브라우저 저장은 보안 1조 금지) — PM 계약 제안 필요
2026-08-02 | AI(Claude) | Vercel 배포 실패 수리 — 실패 배포는 PR #7 브랜치 프리뷰였고, 원인은 /write의 useSearchParams가 정적 프리렌더에서 Suspense 경계를 요구하는 것(dev는 안 터지고 build만 터짐 → 로컬 pnpm build로 재현). Suspense 껍데기 추가. 합류 중 발견 2건: ① doyun의 직행 상태 수리(6ea60c5)와 내 handoff가 충돌 없이 맞물림 ② 인메모리 스토어의 세션 참조 공유 때문에 이번 턴 제안을 handoff가 대기 중으로 오인 — 스토어에 따라 첫 턴 응답이 갈라지는 버그, 이번 턴 제안 제외로 수리(doyun의 route.test가 잡음). 빌드·테스트 44/44 초록
2026-08-02 | AI(Claude) | CLM 1~2단계 + 인증 구멍 차단. getCurrentUserId가 비로그인에도 DEV_USER_ID를 줘서 익명 사용자끼리 같은 신원이었다 — 목록 화면을 켜는 순간 명세 유출이 될 자리였다. 반환 타입을 string|null로 바꾸니 타입 검사가 호출부 8곳을 전부 잡아냈다(사람이면 하나는 놓쳤을 것). 파급으로 e2e가 401에 막혀 로그인 단계를 추가(비밀번호는 .env에서 읽는다). 쌍으로 검증: 비로그인 401 / 로그인 200 / 관통 16단계 PASS
2026-08-02 | AI(Claude) | 실서명 왕복 1건 완료(사람이 서명). 라벨↔필드 매핑이 좌표 추정에서 **실측 확인**으로 바뀜 — 7개 값이 전부 제 칸에, "1,000,000원"·"2026년 8월 2일" 변환도 정상. 전체 점검에서 둘을 잡음: ① 내가 만든 델타 동기화가 상태만 맞추고 증빙을 안 만들어 COMPLETED인데 증빙 없는 상태를 남겼다(FR-505 불변식) — ensureEvidence 추가 후 실데이터로 확인 ② **실웹훅이 한 번도 도착한 적이 없다** — 등록 URL·시크릿은 정상인데 서명 완료 시각에 webhook_events가 비어 있다. 사람이 Vercel 로그로 401 여부를 봐야 갈린다
2026-08-02 | AI(Claude) | git pull 후 원격 main이 빨간불인 것을 발견 — FE 회상 화면 정리(6fb711a)가 커버리지 표시를 접기로 결정했는데 소스 검사(recall.test)가 옛 사용을 요구. 의도(화면이 세지 않는다)는 유지하며 검사를 실체에 맞춤 — 결정 주석이 지워지면 다시 잡히게. chat-resume 때와 같은 계열 두 번째: 화면 개편 시 소스 검사 갱신이 늘 빠진다. Actions만 켜지면 병합 전에 잡힐 일
